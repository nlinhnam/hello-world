There is some downloads that need to be done for python.

To run all 3, run the AI.py first

Then open up the index.html on a browser

It should work. You can see more information by checking the browser console